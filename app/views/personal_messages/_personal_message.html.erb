<% if message.user.name == current_user.name %>
  <%= render 'personal_messages/outgoing_message', { message: message } %>
<% else %>
  <%= render 'personal_messages/incoming_message', { message: message } %>
<% end %>
